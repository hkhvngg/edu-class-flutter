name: demo
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Firebase - Đã cập nhật phiên bản mới nhất
  firebase_core: ^2.32.0
  firebase_auth: ^4.20.0
  cloud_firestore: ^4.17.5
  file_picker: ^8.0.7
  syncfusion_flutter_pdf: ^33.2.5
  google_generative_ai: ^0.4.7
  flutter_bloc: ^9.1.1
  bloc: ^9.2.1
  equatable: ^2.0.8
  firebase_messaging: ^14.9.4
  flutter_local_notifications: ^21.0.0
  googleapis_auth: ^2.3.1
  http: ^1.6.0
  firebase_storage: ^11.7.7
  image_picker: ^1.1.2
  url_launcher: ^6.3.2
  video_player: ^2.9.3
  chewie: ^1.8.7
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
